export function calcSum(num1, num2) {
  const sum = num1 + num2;
  console.log(sum);
  return sum;
}

export function calcMultiplication(num1, num2) {
  const mult = num1 * num2;
  console.log(mult);
  return mult;
}

export function showSumAndMultiplication(num1, num2) {
  calcSum(num1, num2);
  calcMultiplication(num1, num2);
}

// test husky work ++
